spring:
  profiles:
    active: "dev"
  main:
    banner-mode: "off"
  cloud:
    config:
      uri: http://localhost:8888

logging:
  file:
    max-size: 1MB
    max-history: 10
    total-size-cap: 20MB
